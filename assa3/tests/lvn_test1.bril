@main {
  x: int = const 1;
  y: int = const 2;

  a: int = add x y;   # leader
  b: int = add y x;   # same value (commutative) — LVN will map this to 'a'
  c: int = add b b;   # LVN canonicalizes args: becomes 'add a a'
  print c;
  ret;
}