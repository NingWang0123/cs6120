# Loop program to test iterative dataflow
@main(n: int) {
  i: int = const 0;
  sum: int = const 0;

.loop:
  cond: bool = lt i n;
  br cond .body .done;

.body:
  sum: int = add sum i;
  one: int = const 1;
  i: int = add i one;
  jmp .loop;

.done:
  print sum;
  ret;
}